{
  "name": "mechgg",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install && cd client && npm install",
    "build": "cd client && npx vite build",
    "start": "node server/index.js",
    "start:replit": "npm run install:all && cd client && npx vite build && cd .. && NODE_ENV=production node server/index.js"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.0.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.0",
    "morgan": "^1.10.0"
  }
}